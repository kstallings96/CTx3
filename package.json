{
  "name": "ctx3",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "rescore": "node scripts/rescore.mjs",
    "demo": "npm run check:rules && vite build --mode demo && node scripts/bundle-demo.mjs",
    "passwords": "node scripts/set-passwords.mjs",
    "check:rules": "node scripts/check-rules.mjs",
    "record": "node scripts/record-runs.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
